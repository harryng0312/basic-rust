use std::fs::File;
use std::io::{Read, Seek, Write};

fn copy_file() {
    // open src
    // create dest
}

pub fn test_file() {}

#[cfg(test)]
mod tests {
    #[test]
    fn it_works() {
        assert_eq!(2 + 2, 4);
    }
}
